import { ShoppingBag } from 'lucide-react';
import { PlaceholderPage } from '@cloudgatedevs/cloudgate-client/react';

export const Orders = () => (
  <PlaceholderPage
    title="Orders"
    subtitle="A dedicated space for order management."
    icon={ShoppingBag}
    heading="Order management starts here"
    description="This space is ready for your application's orders. Order management hasn't been configured yet."
  />
);
